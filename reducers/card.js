// import {ADD_CARD, REMOVE_CARD} from '../actions/card'

// export const card = (state={card:[]}, action)  => {
//     switch(action.type) {
//             case ADD_CARD: 
//             return state.card.concat(action.card)
//         case REMOVE_CARD:
//            return state.card.filter(action.cardId)
//         default:
//             return state;
//     }
// }